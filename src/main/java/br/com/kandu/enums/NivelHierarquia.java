package br.com.kandu.enums;

/**
 * Define os níveis hierárquicos dos usuários no sistema.
 * Inicialmente com ADM (Administrador do Sistema) e COMUM (Usuário Padrão).
 */
public enum NivelHierarquia {
    ADM,    // Administrador geral do sistema
    COMUM   // Usuário padrão com permissões básicas
}